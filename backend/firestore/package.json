{
  "name": "ai-baby-monitor-firestore-rules",
  "version": "0.1.0",
  "private": true,
  "description": "Firestore security rules + emulator-based unit tests.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project=kidssafecam-test --config ../../firebase.json \"node --test test/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2",
    "firebase-tools": "^15.21.0"
  }
}
